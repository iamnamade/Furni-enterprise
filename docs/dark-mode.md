# Dark Mode Setup

1. `next-themes` provider wraps locale layout.
2. Theme uses `attribute="class"`, `defaultTheme="system"`, `enableSystem`.
3. Manual toggle is in navbar (`ThemeToggle`).
4. Preference persists in `localStorage` via next-themes.
5. Tailwind is configured with `darkMode: ["class"]`.
